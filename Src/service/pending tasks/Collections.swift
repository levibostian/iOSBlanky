import Foundation
import Wendy

enum PendingTaskCollectionId: CollectionId {
    case foo // you must have a case defined in order for protocol to have a raw value type. Replace this case. It's here as just a placeholder.
}
